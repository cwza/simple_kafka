apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-kafka-consumer
spec:
  selector:
    matchLabels:
      app: simple-kafka-consumer
  replicas: 1 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app: simple-kafka-consumer
    spec:
      containers:
      - name: simple-kafka-consumer
        image: cwza/simple_kafka:latest
        imagePullPolicy: Always
        command: ["/consumer"]
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        env:
        - name: ADDRESS
          value: {{ .Values.consumer.address }}
        - name: TOPIC
          value: {{ .Values.consumer.topic }}
        - name: GROUP_ID
          value: {{ .Values.consumer.groupId }}